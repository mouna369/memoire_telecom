remarque 1 :
bach n9der ndir spark-master : sudo nano /etc/hosts  / 127.0.0.1   spark-master /



remarque 2 : mon API cloud 
AIzaSyAqwBb6cJVtzpVedVSDNG97a8N0kWmsa5g




Outil	URL	Contenu
Kafka UI	http://localhost:8088	Messages temps réel
Spark UI	http://localhost:8080	État des workers
MongoDB	via commande	Données
Streamlit	http://localhost:8501	Dashboard final
Prefect	http://localhost:4200	Orchestration


# 1. Se connecter à MongoDB
mongosh "mongodb://localhost:27018/"

# 2. Sélectionner la base de données
use telecom_algerie

# 3. Mettre à jour tous les documents (traite = false)
db.commentaires_sans_urls_arobase.updateMany({}, {$set: {traite: false}})

# 4. Vérifier le résultat
db.commentaires_sans_urls_arobase.countDocuments({traite: false})










# Créer le topic pour les commentaires
docker exec kafka_pfe kafka-topics --create \
  --topic commentaires_bruts \
  --bootstrap-server localhost:9092 \
  --partitions 3 \
  --replication-factor 1

# Vérifier que le topic a été créé
docker exec kafka_pfe kafka-topics --list --bootstrap-server localhost:9092